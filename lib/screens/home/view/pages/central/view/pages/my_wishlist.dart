import 'package:flutter/material.dart';

class MyWishlistView extends StatelessWidget {
  const MyWishlistView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}