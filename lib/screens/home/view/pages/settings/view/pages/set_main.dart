import 'package:flutter/material.dart';

class SetMainView extends StatelessWidget {
  const SetMainView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}