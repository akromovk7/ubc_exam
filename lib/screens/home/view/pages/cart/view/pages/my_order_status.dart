import 'package:flutter/material.dart';

class MyOrderStatusView extends StatelessWidget {
  const MyOrderStatusView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}