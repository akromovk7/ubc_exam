import 'package:flutter/material.dart';

class MyOrderDetailView extends StatelessWidget {
  const MyOrderDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}